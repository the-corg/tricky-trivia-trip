<UserControl x:Class="TrickyTriviaTrip.View.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TrickyTriviaTrip.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{StaticResource GreenGradient}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <!-- Top bar -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid.Resources>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource GameTextSecondary}"/>
                </Grid.Resources>

                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Text="Score: " Margin="8 0 0 0"/>
                    <TextBlock Text="{Binding Score}"/>
                </StackPanel>

                <Button Grid.Column="2" Content="Exit to Menu" Style="{StaticResource MenuButton}"
            Margin="8 4" FontSize="{StaticResource GameSecondaryFontSize}"
            Command="{Binding ExitToMenuCommand}"/>
                <StackPanel Grid.ColumnSpan="3" VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Text="Question "/>
                    <TextBlock Text="{Binding CurrentQuestionNumber}"/>
                    <TextBlock Text=" out of "/>
                    <TextBlock Text="{Binding GameSessionQuestionsTotal}"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="14*"/>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>

                <Grid.Resources>
                    <Style TargetType="Button" BasedOn="{StaticResource GameAnswerButton}"/>
                </Grid.Resources>




                <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding Question.Text}" Style="{StaticResource GameQuestionText}"/>
                <Button Grid.Row="3" Grid.Column="2" Content="{Binding AnswerOption1.Text}" Command="{Binding Answer1Command}"/>
                <Button Grid.Row="4" Grid.Column="2" Content="{Binding AnswerOption2.Text}" Command="{Binding Answer2Command}"/>
                <Button Grid.Row="5" Grid.Column="2" Content="{Binding AnswerOption3.Text}" Command="{Binding Answer3Command}"/>
                <Button Grid.Row="6" Grid.Column="2" Content="{Binding AnswerOption4.Text}" Command="{Binding Answer4Command}"/>

                <Button Grid.Row="7" Grid.Column="2" Content="Next Question ⇨" Margin="0 8" Style="{StaticResource MenuButton}"/>

            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
